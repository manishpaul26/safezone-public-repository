package com.track.safezone.utils;

public class GeneralUtils {

    private void openDialog() {
/*        AlertDialog.Builder dialogBuilder = new AlertDialog.Builder(this);
        View layoutView = getLayoutInflater().inflate(  R.layout.activity_confirmation_screen, null);

        dialogBuilder.setView(layoutView);
        AlertDialog alertDialog = dialogBuilder.create();

        Button nextButton = layoutView.findViewById(R.id.button_send_to_start_quarantine);

        //ImageView tick = layoutView.findViewById(R.id.icon_tick_observation_conditions);
        //        TextView observationStarted = (TextView) findViewById(R.id.text_observation_started);
        alertDialog.getWindow().getAttributes().windowAnimations = R.style.DialogAnimation;
        alertDialog.getWindow().setBackgroundDrawable(new ColorDrawable(Color.TRANSPARENT));
        alertDialog.show();

        nextButton.setOnClickListener(v -> {
            Intent i = new Intent(getApplicationContext(), ConfirmObservationStatusActivity.class);
            startActivity(i);
        });*/
    }
}
